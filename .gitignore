# Compiled Python files
*.pyc

# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

#node module
node_modules

/.config/gcloud/
/.config/gh
# User-customized function files (but track the defaults)
# Only ignore files that aren't in the repo already
# Use: git add -f .functions.d/customfile.sh to force-add custom files if needed

# Secrets files (should never be committed)
.bash_secrets
!.bash_secrets.template

# Backups from setup script
.dotfiles.backup/
