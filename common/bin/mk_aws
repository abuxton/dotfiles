#!/usr/bin/env bash

cat <<EOF > .env
#generated by common/bin/mk_aws
#https://docs.aws.amazon.com/cli/v1/userguide/cli-configure-envvars.html#envvars-list
# AWS_DEFAULT_REGION=
# AWS_PROFILE=
# AWS_SHARED_CREDENTIALS_FILE=/Users/$(whoami)/.aws/credentials

EOF
